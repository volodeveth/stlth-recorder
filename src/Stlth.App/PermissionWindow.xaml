<Window x:Class="Stlth.App.PermissionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Немає доступу до мікрофона"
        SizeToContent="Height"
        Width="450"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Topmost="True">
    <StackPanel Margin="28,24,28,22">

        <TextBlock Style="{StaticResource Title}" Text="Windows не дає доступу до мікрофона" />

        <TextBlock Style="{StaticResource Secondary}" Margin="0,0,0,10">
            Без нього запишеться лише співрозмовник — ваш голос до сесії не потрапить.
        </TextBlock>

        <TextBlock Style="{StaticResource Secondary}" Margin="0,0,0,18">
            Відкрийте «Конфіденційність → Мікрофон» і дозвольте доступ класичним
            застосункам, потім спробуйте ще раз.
        </TextBlock>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Скасувати" IsCancel="True" Click="Cancel_Click" Margin="0,0,10,0" />
            <Button Content="Писати без мікрофона" Click="Continue_Click" Margin="0,0,10,0" MinWidth="160" />
            <Button Content="Відкрити налаштування"
                    IsDefault="True"
                    MinWidth="170"
                    Click="Open_Click" />
        </StackPanel>

    </StackPanel>
</Window>
